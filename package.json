{
  "name": "photography-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "docker:up": "docker compose up -d",
    "docker:up:build": "docker compose up -d --build",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f",
    "docker:logs:api": "docker compose logs -f api",
    "docker:logs:web": "docker compose logs -f web",
    "api:exec": "docker compose exec api",
    "api:prisma:migrate": "docker compose exec api npx prisma migrate dev",
    "api:prisma:studio": "docker compose exec api npx prisma studio",
    "api:prisma:push": "docker compose exec api npx prisma db push"
  }
}
